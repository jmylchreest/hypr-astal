// This file is the entry point for config. When using dotter, this file
// is replaced by a template that merges machine-specific overrides.
// For standalone use, it just re-exports the defaults.
export { default } from "./config.defaults"
